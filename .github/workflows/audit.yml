name: Audit

on: push

jobs:
  audit:
    runs-on: ubuntu-latest
    name: Audit

    steps:
      # - name: Git checkout
      #   uses: actions/checkout@v4

      - name: Install modules
        run: npm i
        working-directory: ../

      - name: ESLint validation
        run: npx eslint .
        working-directory: ../
