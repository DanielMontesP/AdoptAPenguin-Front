name: Audit

on: push

jobs:
  audit:
    runs-on: ubuntu-latest
    name: Audit

    steps:
      # - name: Git checkout
      #   uses: actions/checkout@v4

      - name: Install modules
        run: npm i
        working-directory: "../AdoptAPenguin 3.0"

      - name: ESLint validation
        run: npx eslint .
        working-directory: "../AdoptAPenguin 3.0"
