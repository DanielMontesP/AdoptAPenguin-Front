export interface UISliceState {
  loading: boolean;
  modalMessage: string;
  modalType: string;
  headerTitle: string;
  headerLastTitle: string;
  isDesktop: boolean;
  stringToSearch: string;
  isMenuOpen: boolean;
  isModalOpen: boolean;
  isSearchOpen: boolean;
}
